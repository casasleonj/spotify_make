<h1>Start Yeah, Show de un artista en particular Make it Real Albums!</h1>
<h2><%= @artist.name %></h2>

<ul>
  <% @artist.albums.each do |album| %>
    <li><%= album.name %></li>
    <img src="<%= album.image_url %>" alt="">
    <li><%= album.released_at %></li>
  <% end %>
</ul>